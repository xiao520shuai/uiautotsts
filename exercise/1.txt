登陆ssh root@121.37.188.251
密码！@#￥ws981124
服务器地址：121.37.188.251
katalon：921434152@qq.com
               Ws981124.	
bug：
和测试员预期不一致的软件行为
和程序员预期不一致的软件行为
和文档预期不一致的软件行为
和管理者预期不一致的软件行为
和客户预期不一致的软件行为
什么是docker：
通过镜像提供软件运行需要的条件，包括代码、运行时环境、系统
工具、系统库、设置等，在镜像上拉取容器，保障测试环境和开发
环境的一致性，减少因环境导致的运行软件冲突
docker原理：
Docker容器的实现原理就是通过Namespace命名空间实现进程隔
离、UnionFilesystem联合文件系统实现文件系统隔离、
ControlGroups控制组实现资源隔离
docker命令：
docker pull 地址把程序从远程拉到本地
docker run是在本地把拉到本地的image(镜像)在容器中运行
-p 3000(对外端口，在浏览器上访问的):3000（程序自带的端口不
需要改）
linux命令：
uname -r 显示正在使用的内核版本 
ls — List ls会列举出当前工作目录的内容（文件或文件夹）
mkdir — Make Directory  mkdir 用于新建一个新目录
pwd — Print Working Directory 显示当前工作目录
协议：
HTTP 请求由三个部分构成，分别是：
方法-URI-协议/版本，请求头，请求正文
1.请求头：
通用头标：即可用于请求，也可用于响应，是作为一个整体而不是
特定资源与事务相关联。
请求头标：允许客户端传递关于自身的信息和希望的响应形式。
响应头标：服务器和于传递自身信息的响应。
实体头标：定义被传送资源的信息。即可用于请求，也可用于响应
2.请求方法：GET，POST，HEAD，OPTIONS，PUT，DELETE，
TRACE
3.请示正文：请示原由、请示事项、请求批复三部分
HTTP 应答也由三个部分构成，分别是：协议-状态代码-描述，应
答头，应答正文
HTTP 应答的第一行类似于 HTTP 请求的第一行，它表示通信所用
的协议是 HTTP 1.1，服务器已经成功地处理了客户端发出的请求
200 表示成功
应答头也和请求头一样包含许多有用的信息，例如服务器类型、日
期时间、内容类型和长度等。应答的正文就是服务器返回的 HTML 
页面。应答头和正文之间也用 CRLF 分隔。












































